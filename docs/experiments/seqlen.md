---
title: 序列长度敏感性分析
---

# 序列长度敏感性分析

为分析序列长度对 MIMO-WM 预测精度的影响，在 Humanoid 和 HumanoidStandup 两个数据集上测试了不同序列长度 $T \in \{8, 16, 32, 64, 128\}$ 下的性能。所有结果为 3 个种子的均值 ± 标准差。

## 实验结果

| $T$ | Humanoid MSE ($\times 10^{-2}$) | Humanoid $R^2$ | HumanoidStandup MSE ($\times 10^{-2}$) | HumanoidStandup $R^2$ |
|-----|--------------------------------|----------------|----------------------------------------|----------------------|
| **8** | **20.14 ± 0.13** | **0.765** | **50.02 ± 0.03** | **0.478** |
| 16 | 19.23 ± 0.14 | 0.764 | 51.29 ± 0.11 | 0.461 |
| 32 | 21.18 ± 0.04 | 0.735 | 54.65 ± 0.04 | 0.428 |
| 64 | 21.28 ± 0.16 | 0.708 | 58.80 ± 0.11 | 0.380 |
| 128 | 41.13 ± 0.36 | 0.448 | 64.00 ± 0.05 | 0.320 |

## 关键发现

!!! success "T=8 在两个数据集上均取得最优 R²"
    - **Humanoid**：$T=8$ 时 $R^2 = 0.765$ 最优，MSE 为 20.14；
    - **HumanoidStandup**：$T=8$ 时 $R^2 = 0.478$ 最优，MSE 为 50.02。

    两个数据集均在 $T=8$ 时取得最优 $R^2$，且预测精度（$R^2$）随 $T$ 增大而 **单调下降**。

### 精度随序列长度的变化趋势

```
R²
0.80 ┤                              ●  T=8   ← 最优
0.76 ┤                          ●      T=16
0.74 ┤              ●               T=32（默认）
0.70 ┤                                  T=64
0.60 ┤
0.50 ┤
0.45 ┤                                          ●  T=128
0.40 ┤
     └──────────────────────────────────────────
        8      16      32      64     128        T
```

### 精度退化解读

| $T$ 区间 | $R^2$ 变化（Humanoid） | 原因 |
|---------|----------------------|------|
| 8 → 16 | 0.765 → 0.764（持平） | 短序列已捕捉主要动力学 |
| 16 → 32 | 0.764 → 0.735（−3.8%） | 中等序列引入噪声步 |
| 32 → 64 | 0.735 → 0.708（−3.7%） | 长序列累积无关信息 |
| 64 → 128 | 0.708 → 0.448（−38.8%） | 过长序列严重稀释信号 |

## 为什么短序列更好？

!!! info "高维人形机器人动力学的特点"
    对于高维人形机器人动力学系统，**较短的序列长度更有利于预测**，原因有三：

    1. **马尔可夫性近似**。人形机器人状态（关节角、角速度、机身位姿）已包含丰富的动力学信息，短历史即可近似马尔可夫性，过长历史带来的边际信息很少。

    2. **噪声累积**。长序列中，远处时间步的状态波动对下一刻预测的相关性低，反而引入噪声，稀释了模型的注意力。

    3. **接触动力学的局部性**。MuJoCo 接触/碰撞响应主要依赖近端状态，长时间尺度的远端历史对接触预测帮助有限。

!!! warning "主实验仍用 T=32 的原因"
    虽然序列长度分析显示 $T=8$ 最优，但主对比实验（[状态预测结果](results.md)）统一采用 $T=32$ 以确保 **所有六种基线方法在相同设置下公平比较**——$T$ 是所有模型共享的训练超参数，而非 MIMO-WM 特有。该分析则专门刻画 MIMO-WM 自身对 $T$ 的敏感性。

## 推荐配置

| 任务类型 | 推荐 $T$ | 说明 |
|---------|---------|------|
| 在线状态预测 | $T \in [8, 16]$ | 精度最优 |
| 多模型公平对比 | $T = 32$ | 与基线统一 |
| 长时序分析（研究用） | $T \leq 64$ | 避免 $T=128$ 的严重退化 |

!!! summary "结论"
    对于高维人形机器人动力学系统，**较短的序列长度更有利于预测**，推荐 $T \in [8, 16]$。序列长度 $T$ 是一个被低估的关键超参数——在 MIMO-WM 上，从 $T=128$ 调整到 $T=8$ 可使 $R^2$ 从 0.448 提升到 0.765（Humanoid），提升幅度远超许多架构层面的改进。

---

上一节：[消融实验](ablation.md) · 下一节：[MPC 实验](mpc.md)
